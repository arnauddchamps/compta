{% extends 'base.html.twig' %}

{% block title %}Hello IndexController!
{% endblock %}

{% block body %}
	<section id="home">
		<div class="container">
			<header>
			</header>
			<section>
				<div class="row">
					<div class="col-4">
						<div class="card">
							<div class="card-header"><h4>Catégorie</h4></div>
							<div class="card-body">
								<a href="{{path('category_list')}}">Liste des catégories</a>
								<a href="{{path('category_new')}}">Ajouter une catégorie</a>
							</div>
						</div>
					</div>
                    <div class="col-4">
						<div class="card">
							<div class="card-header"><h4>Ligne bancaire</h4></div>
							<div class="card-body">
								<a href="{{path('line_list')}}">Listes des entrées bancaires</a>
								<a href="{{path('line_new')}}">Ajouter une entrée bancaire</a>
							</div>
						</div>
					</div>
				</div>
			</section>
		</div>
	</section>
{% endblock %}
